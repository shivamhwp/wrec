[package]
name = "backend"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
config = { path = "../config" }
domain = { package = "core", path = "../core" }
store = { path = "../store" }
tracing.workspace = true
